redis:
  host: 192.168.101.101
  port: 6379
  password: lemerence
  timeout: 3000  # 连接超时时间（毫秒）默认是2000ms
  max-active: 500  #最大连接数据库连接数,设 0 为没有限制
  max-wait: -1  # 连接池最大阻塞等待时间（使用负值表示没有限制）
  max-idle: 50 #最大建立连接等待时间。如果超过此时间将接到异常。设为-1表示无限制。
  min-idle: 0 #最小等待连接中的数量,设 0 为没有限制